{
  "crons": [
    {
      "path": "/api/cron/hit-urls?scheduleType=daily",
      "schedule": "0 0 * * *",
      "headers": {
        "Authorization": "Bearer ${process.env.CRON_SECRET}"
      }
    }
  ]
}